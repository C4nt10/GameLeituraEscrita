# D-06 / D-54 — tocar numa letra errada não some com as alternativas nem pune: o bloco só treme e as
# 4 letras continuam ali. Rota `_dev/ditado-nivel1` (nível 1, 4 alternativas).
#
# NÃO EXECUTADO — sem Maestro CLI nesta máquina (Android SDK/emulador sim, mas o
# Maestro nunca foi instalado). Escrito contra o app do padrão visual Letra Viva
# (T119): os textos foram conferidos contra o código atual; seletores e fluxos
# nunca rodaram (ex.: se `longPressOn` casa com o rótulo de acessibilidade da
# engrenagem, se `tapOn: 'Nome'` acha o campo). Rotas `_dev` abrem por link
# direto (`gameleituraescrita://`).

appId: com.devirlabs.gameleituraescrita
---
- launchApp
- openLink: gameleituraescrita://_dev/ditado-nivel1
- assertVisible:
    text: '^[A-Z]$'
    index: 0
- tapOn:
    text: '^[A-Z]$'
    index: 0
# se a letra tocada era a errada, as 4 continuam; se era a certa, aparece "Isso!" (ordem aleatória)
- assertVisible:
    text: '^[A-Z]$'
    index: 1
- assertNotVisible: 'Erro'
- assertNotVisible: 'Errou'
